# Dependencies for the website in docs/. Loosely pinned on purpose: the site is
# rebuilt only when a release goes out, so it should pick up fixes in between.
# Pin exactly if a build ever breaks on a new release of either package.
mkdocs>=1.6.1,<2
mkdocs-material>=9.6,<10
