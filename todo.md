# TODO

## Done

- [x] Dutch comments → English (extension, tests, build scripts)
- [x] Dutch check() labels → English (test-core / options / popup)
- [x] Harness meta (`expected` / `got` / `TOTAL`) + `run-tests.ps1` matchers

## Leave Dutch on purpose

- [ ] **`store/*.txt`.** Deliberately Dutch — leave.
- [ ] **`_locales/nl/messages.json`.** Must stay Dutch.

## Not part of this

The `BCEM` namespace and the `bcem-` prefixes on CSS classes, alarm names and
message types (`bcem:sync`) are identifiers, not language.

Leave them alone unless you decide to rename deliberately.
